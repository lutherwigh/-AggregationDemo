<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="outlook.luxi96.module_navi">

    <application
        android:name="moe.luther.library.base.debug.DebugApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/navi_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NaviTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="outlook.luxi96.module_navi.debug.DebugActivity">

        </activity>
        <activity android:name="outlook.luxi96.module_navi.NaviActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>